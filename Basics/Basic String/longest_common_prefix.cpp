#include<iostream> 
#include<vector>
#include<string>
#include<algorithm>

using namespace std;

string longestCommonPrefix(vector<string>& str){
    vector<char> chars;
    sort(str.begin(), str.end());
    return ans;


}
int main(){
    int n;
    vector<string> str;
    cin >> n;

    for(int i=0; i<n; i++){
        cin >> words[i];
    }
    return 0;
}